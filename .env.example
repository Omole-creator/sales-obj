# Meta (Facebook) Pixel ID — the ~15-16 digit number from Events Manager.
# Copy this file to `.env.local` and paste your real ID. Leave empty to disable the pixel.
# In production, set this in Vercel → Project → Settings → Environment Variables.
NEXT_PUBLIC_FB_PIXEL_ID=
